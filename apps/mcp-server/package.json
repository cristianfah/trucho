{
  "name": "@trucho/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "Servidor MCP de Trucho (Fase 2 — aún no implementado)",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "echo 'Fase 2: pendiente' && exit 0",
    "typecheck": "echo 'Fase 2: pendiente' && exit 0"
  }
}
